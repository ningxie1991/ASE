apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: load-balancer
spec:
  rules:
  - http:
      paths:
      - path: /api/finder/
        backend:
          serviceName: finder-service
          servicePort: 3001
      